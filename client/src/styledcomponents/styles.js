import { Header, HeaderProps, Menu, MenuProps } from 'semantic-ui-react'
import styled, { StyledComponentClass } from 'styled-components'

export const AppContainer = styled.div`
  background-color: blue;
  height: 100% !important;
`
